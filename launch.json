{
    "configurations": [
        {
            "type": "msedge",
            "name": "https://localhost:5000",
            "request": "launch",
            "url": "https://localhost:5000"
        }
    ]
}